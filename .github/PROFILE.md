<!-- Rendered into the profile README at github.com/BU1lDR by
     BU1lDR/BU1lDR/tools/build_readme.py, on a push here and once a day regardless.
     Format: "# <display name> — <heading tail>", a one-line meta row, then at most
     two short paragraphs. Placeholders filled from the GitHub API: {license}
     {version} {live} {description}. Keep it brief; the detail belongs in README.md,
     which the deploy's checks hold to the site. -->

# Portfolio — hand-written site, no framework and no build step ([live]({live}))

{license} · HTML · CSS · JavaScript · GitHub Pages

A wireframe torii gate drawn on a 2D canvas through a hand-rolled perspective projection, an in-page shell with command history, tab completion and typo suggestions, and an easter-egg scoreboard. Reduced-motion support, keyboard-only navigation, an `aria-live` terminal log, JSON-LD and Open Graph metadata.

The deploy fails on a Content-Security-Policy that admits anything third-party beyond Google Fonts, on a dead local link or anchor, on a drifted line number in the README's setup table, and on a command count that disagrees with the shell. Weekly jobs check external links and hold the résumé's secscan test count to the figure secscan publishes.
