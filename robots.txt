# robots.txt is read per ORIGIN, from the root path only:
#
#     https://bu1ldr.github.io/robots.txt
#
# This is a project site published under /Portfolio/, so the copy you are
# reading is not the one crawlers fetch. That matters in exactly one direction:
# a Disallow: added here will look correct — valid syntax, file reachable, no
# error reported anywhere — and will keep nothing out. To keep a page out of
# search results, put the tag in the document itself:
#
#     <meta name="robots" content="noindex, nofollow">
#
# which works at any path. assets/resume.src.html already does that.
#
# Nothing is lost in the meantime: the directives below allow everything, which
# is also what having no robots.txt means. Submit the sitemap URL in Search
# Console to get it indexed — that route does not depend on this file.

User-agent: *
Allow: /

Sitemap: https://bu1ldr.github.io/Portfolio/sitemap.xml
